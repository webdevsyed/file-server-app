import { createContext } from "react"

const UserContext = createContext({
    userData: {
        // email: "email",
        // name: "name",
        // googleId: "sub",
        // picture: "picture"
    }
})


export default UserContext